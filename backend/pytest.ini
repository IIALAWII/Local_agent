[pytest]
# Run async test functions automatically without requiring @pytest.mark.asyncio
asyncio_mode = auto

# Show short tracebacks and verbose output by default
addopts = -v --tb=short

testpaths = tests
